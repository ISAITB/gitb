{
    "1111090": {
        "active": true,
        "notes": "SVGs are never handled with Angular template interpolation."
    },
    "1111181": {
        "active": true,
        "notes": "SVGs are never handled with Angular template interpolation."
    },
    "1112034": {
        "active": true,
        "notes": "SVGs are never handled with Angular template interpolation."
    },
    "1112035": {
        "active": true,
        "notes": "SVGs are never handled with Angular template interpolation."
    },
    "1112157": {
        "active": true,
        "notes": "SVGs are never handled with Angular template interpolation."
    },
    "1112158": {
        "active": true,
        "notes": "SVGs are never handled with Angular template interpolation."
    },
    "1112455": {
        "active": true,
        "notes": "The lodash functions in question are not used and lodash is removed from the development branch."
    },
    "1112453": {
        "active": true,
        "notes": "The lodash functions in question are not used and lodash is removed from the development branch."
    },
    "1113588": {
        "active": true,
        "notes": "Angular i18n is not used to load translations and in any case if translations were used they would be loaded by trusted sources."
    },
    "1113979": {
        "active": true,
        "notes": "Dompurify is defined as a dependency of mermaid, a dependency of mermaid which is in turn an optional dependency of ngx-markdown. Mermaid is never used but also there is never any processing or untrusted markdown."
    },
    "1113981": {
        "active": true,
        "notes": "Dompurify is defined as a dependency of mermaid, a dependency of mermaid which is in turn an optional dependency of ngx-markdown. Mermaid is never used but also there is never any processing or untrusted markdown."
    },
    "1114017": {
        "active": true,
        "notes": "Dompurify is defined as a dependency of mermaid, a dependency of mermaid which is in turn an optional dependency of ngx-markdown. Mermaid is never used but also there is never any processing or untrusted markdown."
    }
}
